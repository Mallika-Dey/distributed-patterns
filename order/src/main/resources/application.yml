server:
  port: 8083

spring:
  application:
    name: order
  datasource:
    url: jdbc:postgresql://localhost:5432/order
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update

